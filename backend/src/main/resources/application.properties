spring.application.name=metamapa
app.base-url=http://56.126.5.148:8080/api
proxy.base-url=https://api-ddsi.disilab.ar/public/api

limite.dias.edicion=7
hecho.estado.por.defecto=PENDIENTE

# Configuraci?n de la base de datos
spring.datasource.url=jdbc:mysql://basededatosdds.c5sg6mesexie.sa-east-1.rds.amazonaws.com:3306/metamapa_db?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=basededatitos2025!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuraci?n de sincronizaci?n de entities contra la base de datos
spring.jpa.hibernate.ddl-auto=update
# none: no toca la BD.
# validate: valida que las entidades coincidan con la BD.
# update: actualiza la BD seg?n las entidades (muy usado en desarrollo).
# create: crea las tablas en cada inicio.
# create-drop: igual que create pero borra al apagar.

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Subirlo al repo como "application.properties.example" con valores genericos
# para no subir valores sensibles

# Ubicación física del filesystem
#upload.path=/var/app/uploads
upload.path=${user.home}/uploads
# o para desarrollo: upload.path=${user.home}/uploads

# Límites de carga
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=25MB

#logging.level.root=DEBUG
# Ruta donde Spring Boot escribe el log
logging.file.name=/home/ubuntu/logs/backend.log

# Nivel de log (opcional)
#logging.level.root=INFO

dd.logs.injection=true
dd.service=backend_metamapa
dd.env=dev


logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.web.multipart=DEBUG
